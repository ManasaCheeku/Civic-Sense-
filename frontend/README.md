# CivicSense AI Frontend

This frontend is a Vite + React application for the CivicSense AI compliance platform.

## Development

```bash
npm install
npm run dev
```

## Production Build

```bash
npm run build
```

The app expects the backend API to be available at the URL configured by `VITE_API_URL` or `http://localhost:8000` by default.
